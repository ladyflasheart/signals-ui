import React, { Component } from 'react'
import PropTypes from 'prop-types'

class Signal extends Component {
  render () {
    return (
      <div>
        <Light />
        <Answer />
      </div>
    )
  }
}

Signal.propTypes = {colour:}

export default Signal
